#Install commands
pip install -r requirements.txt
pip install python-telegram-bot --upgrade

#Use command
python share.py file.txt
python share.py *
python share.py /user/home/bin/file.txt

#Share file to public. 
Add your bot to any group as admin and provide it all the permissions. 
And share the file same way.
